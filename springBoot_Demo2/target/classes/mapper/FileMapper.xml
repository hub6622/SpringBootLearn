<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="ppg.demo.mapper.FileMapper">
    <insert id="addFile" parameterType="ppg.demo.pojo.UserFile">
        insert into t_file(file_name)
        select file_name from t_file
        where not exists
        (select * from t_file where file_name = #{fileName})
    </insert>
</mapper>
